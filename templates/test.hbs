<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; padding: 40px; }
    h1 { color: #333; }
  </style>
</head>
<body>
  <h1>{{companyName}}</h1>
  <p>Instalado por: {{technicianName}}</p>
  <p>Fecha: {{installDate}}</p>
  {{#if signature}}
    <h3>Firma del cliente:</h3>
    <img src="{{signature}}" width="300"/>
  {{/if}}
</body>
</html>